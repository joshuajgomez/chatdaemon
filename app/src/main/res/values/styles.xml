<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="GreenTheme" parent="AppTheme">
        <item name="colorBackgroundPrimary">@color/green</item>
        <item name="colorBackgroundSecondary">@color/white</item>
        <item name="colorAccent">@color/black</item>
        <item name="colorTextPrimary">@color/white</item>
        <item name="colorTextSecondary">@color/black</item>
        <item name="colorProgressBar">@color/white</item>
        <item name="colorButtonBackground">@color/black</item>
        <item name="colorButtonText">@color/green</item>
    </style>

</resources>
