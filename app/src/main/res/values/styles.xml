<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="GreenTheme" parent="AppTheme">
        <item name="colorBackgroundPrimary">@color/white</item>
        <item name="colorBackgroundSecondary">@color/white</item>
        <item name="colorAccent">@color/black</item>
        <item name="colorTextPrimary">@color/black</item>
        <item name="colorTextSecondary">@color/green</item>
        <item name="colorProgressBar">@color/green</item>
        <item name="colorButtonBackground">@color/black</item>
        <item name="colorButtonText">@color/white</item>

        <item name="imageDefaultUser">@mipmap/ic_user_blue</item>

        <item name="actionBarHeight">60dp</item>
    </style>

</resources>
